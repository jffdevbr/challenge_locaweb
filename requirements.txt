# Versões PINADAS, e isso não é zelo: os artefatos de models/ são pickle de statsmodels, e a
# versão que desserializa precisa casar com a que serializou — senão a carga quebra ou, pior,
# carrega errado. Os pins abaixo são exatamente o ambiente que treinou (ver /health).
numpy==2.0.2
pandas==2.2.2
scipy==1.16.3
statsmodels==0.14.6

fastapi==0.115.6
uvicorn[standard]==0.32.1

# Ausentes de propósito, e é o que segura a imagem: torch, prophet, cmdstanpy, xgboost,
# scikit-learn, matplotlib. Nenhum modelo vencedor precisa deles — os 18 artefatos são todos
# ARIMA/SARIMA do statsmodels.
