# Versões PINADAS, e isso não é zelo: os artefatos de models/ são pickle de statsmodels, e a
# versão que desserializa precisa casar com a que serializou — senão a carga quebra ou, pior,
# carrega errado. Os pins abaixo são exatamente o ambiente que treinou (ver /health).
numpy==2.0.2
pandas==2.2.2
scipy==1.16.3
statsmodels==0.14.6

fastapi==0.141.1
uvicorn[standard]==0.52.4

# Monitoramento (Azure Monitor / Application Insights) — ver api/main.py.
# O exportador (pacote separado, versionado em beta) pinado explicitamente: da b53 em diante ele
# se recusa a seguir o redirecionamento legítimo do endpoint global de ingestão para o regional —
# "Refusing cross-origin redirect" no log — e TODA telemetria é descartada (nenhuma exceção,
# nenhum erro visível, só silêncio na UI do Azure Monitor). b50 é a última versão sem o bug.
azure-monitor-opentelemetry==1.6.4
azure-monitor-opentelemetry-exporter==1.0.0b50

# Ausentes de propósito, e é o que segura a imagem: torch, prophet, cmdstanpy, xgboost,
# scikit-learn, matplotlib. Nenhum modelo vencedor precisa deles — os 18 artefatos são SARIMAX,
# ETS e Theta, todos do statsmodels.
